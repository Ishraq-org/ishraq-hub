# design/theme-explorer

Drop your interactive HTML palette-explorer tool (the one built with Claude,
Sandalwood Folio etc.) here as the source of truth for the theme, along with any
exported CSS variable blocks (`:root` + `[data-theme="dark"]`).
