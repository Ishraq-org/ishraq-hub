# apps/api

Node + Express backend — placeholder folder.

Will hold the REST API and the Telegram bot (in `src/bot/`), running as one service
per the "one Render service" decision. Nothing scaffolded yet — confirm architecture
first.
