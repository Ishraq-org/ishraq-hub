# apps/web

React + Vite frontend — placeholder folder.

Nothing scaffolded yet (no package.json, no framework installed). This will hold:
public site, article reader, dashboard, admin panel — once the architecture is
confirmed in `docs/architecture/` and logged in `docs/roadmap/decisions-log.md`.
