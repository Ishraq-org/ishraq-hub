{
  "name": "ishraq-hub-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:web": "npm --workspace=apps/web run dev",
    "dev:api": "npm --workspace=apps/api run dev",
    "build:web": "npm --workspace=apps/web run build",
    "build:api": "npm --workspace=apps/api run build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
