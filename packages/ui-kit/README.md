# packages/ui-kit

Shared design primitives: the custom Icon component (no emojis — see project
philosophy), design tokens, and any other component meant to be reused across
web/admin without duplication.
