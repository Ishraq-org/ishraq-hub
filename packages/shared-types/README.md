# packages/shared-types

TypeScript types shared between apps/web and apps/api (Article, User, Topic,
Resource, etc.) so the frontend and backend never drift out of sync on shape.
Populate once the data models are finalized.
